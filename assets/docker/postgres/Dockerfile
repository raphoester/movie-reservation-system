# Plain Postgres for local development. No PostGIS — the movie-reservation
# domain has no geospatial needs. Pin a fixed version so every machine and CI
# run the same engine.
FROM postgres:18.3-alpine3.23
